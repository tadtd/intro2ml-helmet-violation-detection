# .env.example — Copy file này thành .env và điền thông tin thực
# KHÔNG commit file .env lên git

# Supabase Test Project (helmet-violation-test)
# Lấy từ: Supabase Dashboard -> Project Settings -> Database -> Connection string (Transaction pooler)
DB_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres
