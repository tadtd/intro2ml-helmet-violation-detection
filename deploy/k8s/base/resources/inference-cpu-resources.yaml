apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference-worker
spec:
  template:
    spec:
      containers:
        - name: inference-worker
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "2"
              memory: 4Gi
