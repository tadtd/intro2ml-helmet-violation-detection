apiVersion: v1
kind: Service
metadata:
  name: dashboard
  labels:
    app.kubernetes.io/component: dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: dashboard
  ports:
    - name: http
      port: 8004
      targetPort: http
